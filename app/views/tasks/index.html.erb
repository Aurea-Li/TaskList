
<div class="index-task-page">

  <ul>

    <% @tasks.each do |task| %>

    <li>
      <strong> <%= task.name %> : <%= task.description %> </strong>
      <%= link_to "(View Details)", task_path(task.id) %>
      <%= link_to "(Edit Task)", edit_task_path(task.id) %>
    </li>

    <% end %>

  </ul>

  <%= link_to "New Task", new_task_path %>

</div>
